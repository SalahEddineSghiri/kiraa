import { pgTable, text, integer, numeric, date, timestamp, jsonb, uuid, customType } from "drizzle-orm/pg-core";
import { createInsertSchema, createSelectSchema } from "drizzle-zod";
const vector = customType<{data:number[];driver:string}>({ dataType: () => "vector(1536)", toDriver: v => `[${v.join(",")}]`, fromDriver: v => JSON.parse(String(v)) });
export const fleetCatalog=pgTable("fleet_catalog",{id:text("id").primaryKey(),make:text("make").notNull(),model:text("model").notNull(),year:integer("year").notNull(),category:text("category").notNull(),transmission:text("transmission"),fuelType:text("fuel_type"),baseDailyRate:numeric("base_daily_rate",{precision:10,scale:2}).notNull(),vehiclesAvailable:integer("vehicles_available").notNull(),location:text("location")});
export const customerProfiles=pgTable("customer_profiles",{id:text("id").primaryKey(),fullName:text("full_name").notNull(),birthDate:date("birth_date").notNull(),licenseNumber:text("license_number"),licenseIssueDate:date("license_issue_date").notNull(),licenseExpDate:date("license_exp_date").notNull(),riskCategory:text("risk_category")});
export const bookingLogs=pgTable("booking_logs",{id:text("id").primaryKey(),customerId:text("customer_id"),vehicleId:text("vehicle_id").notNull(),startDate:date("start_date").notNull(),endDate:date("end_date").notNull(),days:integer("days").notNull(),insuranceOption:text("insurance_option"),depositAmount:numeric("deposit_amount",{precision:10,scale:2}),discountCode:text("discount_code"),seasonalMultiplier:numeric("seasonal_multiplier",{precision:4,scale:2}),status:text("status").default("CONFIRMED")});
export const seasonalPricingMatrix=pgTable("seasonal_pricing_matrix",{id:uuid("id").defaultRandom().primaryKey(),month:integer("month").notNull(),category:text("category").notNull(),multiplier:numeric("multiplier",{precision:4,scale:2}).notNull()});
export const rentalPoliciesVectors=pgTable("rental_policies_vectors",{id:uuid("id").defaultRandom().primaryKey(),content:text("content").notNull(),source:text("source").notNull(),embedding:vector("embedding").notNull(),createdAt:timestamp("created_at").defaultNow().notNull()});
export const agentRequests=pgTable("agent_requests",{id:text("id").primaryKey(),state:jsonb("state").notNull(),createdAt:timestamp("created_at").defaultNow().notNull()});
export const insertFleetSchema=createInsertSchema(fleetCatalog); export const fleetSchema=createSelectSchema(fleetCatalog);
