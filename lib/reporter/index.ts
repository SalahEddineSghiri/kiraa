import PDFDocument from "pdfkit";
/** Generates a quote only from an already validated deterministic state. */
export async function generateQuotePdf(input:{requestId:string;validation:{isValid:boolean|null;errors:string[]};priceResult:Record<string,unknown>;bookingStatus:string}){if(input.validation.isValid!==true)throw new Error("A quote cannot be generated for an unvalidated request");return new Promise<Buffer>((resolve,reject)=>{const doc=new PDFDocument({margin:48});const chunks:Buffer[]=[];doc.on("data",c=>chunks.push(c));doc.on("end",()=>resolve(Buffer.concat(chunks)));doc.on("error",reject);doc.fontSize(20).text("Kiraa - Devis de location");doc.moveDown().fontSize(11).text(`Demande: ${input.requestId}`);doc.text(`Statut: ${input.bookingStatus}`);doc.moveDown().text("Résultat déterministe");for(const [key,value] of Object.entries(input.priceResult))doc.text(`${key}: ${String(value)}`);doc.end()})}
