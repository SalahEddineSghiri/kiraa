import { describe,it,expect } from "vitest"; import { calculateMileagePenalty,calculateTotalPrice,checkVehicleAvailability,verifyDriverEligibility } from "@/lib/engine";
const economy={id:"VH-1",make:"Test",model:"Economy",category:"Economy" as const,baseDailyRate:300}; const premium={...economy,id:"VH-P",category:"Premium" as const};
describe("notebook deterministic parity",()=>{it("rejects an underage driver",()=>{const r=verifyDriverEligibility("2007-03-10","2025-06-15","2035-06-15");expect(r).toMatchObject({eligible:false,age:19})});it("blocks an expired license",()=>expect(verifyDriverEligibility("1985-11-22","2010-01-10","2026-01-10")).toMatchObject({eligible:false,licenseExpired:true}));it("applies young Premium HITL and 50% deposit",()=>{const e=verifyDriverEligibility("2004-02-18","2023-09-01","2033-09-01",undefined,"Premium");const p=calculateTotalPrice(premium,5,8,"all_risk","",e.riskCategory,1.3);expect(e.needsHumanReview).toBe(true);expect(p.deposit).toBe(22500);expect(p.needsHumanReview).toBe(true)});it("caps SUMMER20 discount at 15%",()=>{const p=calculateTotalPrice(economy,5,7,"basic","SUMMER20","standard",1.3);expect(p.discountCapped).toBe(true);expect(p.discountAmount).toBe(292.5);expect(p.totalPrice).toBe(3957.5)});it("uses strict date overlap and exact mileage rate",()=>{expect(checkVehicleAvailability("VH-1","2026-07-20","2026-07-25",[{vehicleId:"VH-1",startDate:"2026-07-10",endDate:"2026-07-20"}]).available).toBe(true);expect(calculateMileagePenalty(1600,1500)).toMatchObject({kmOverage:100,penalty:250})})});
